fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
toml>=0.10,<1.0
jinja2>=3.1,<4.0

# Swap in / add the real PR-Agent AI + git-provider stack when wiring
# build_agent() in prompting_server.py to production classes:
# litellm>=1.40
# PyGithub>=2.3
